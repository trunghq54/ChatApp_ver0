<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat Client" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="messagesList" Grid.Row="0" Margin="10"/>
        
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="messageTextBox" Grid.Column="0" Margin="0,0,10,0"/>
            <Button x:Name="emojiButton" Grid.Column="1" Content="😊" Margin="0,0,10,0" Width="40"/>
            <Button x:Name="sendButton" Grid.Column="2" Content="Send" Width="70"/>
        </Grid>
    </Grid>
</Window>